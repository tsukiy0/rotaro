{
  "name": "@rotaro/backend",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@rotaro/tools": "*",
    "dotenv": "^8.2.0"
  },
  "dependencies": {
    "@rotaro/core": "*",
    "@tsukiy0/tscore": "1.0.0",
    "aws-sdk": "2.765.0"
  },
  "scripts": {
    "typecheck": "tsc",
    "lint": "eslint \"./src/**/*.{ts,tsx}\"",
    "lint:fix": "yarn lint --fix",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "tsc --build .",
    "test": "jest",
    "test:integration": "jest --config jest.integration.config.js"
  }
}
